{
  "name": "sadykoff",
  "version": "1.0.2",
  "description": "CLI для добавления функционала в Next.js",
  "bin": {
    "sadykoff": "bin/index.js"
  },
  "scripts": {
    "release": "standard-version",
    "postrelease": "git push --follow-tags origin main && npm publish"
  },
  "type": "commonjs",
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
